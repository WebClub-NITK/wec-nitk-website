/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['source.unsplash.com', 'localhost', '3.26.93.132', 'webclub.nitk.ac.in'],
    },
};

export default nextConfig;
